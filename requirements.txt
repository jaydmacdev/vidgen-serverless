# VidGen Minimal Dependencies
# Only what's needed (PyTorch already in base image)

runpod==1.6.2
diffusers==0.31.0
transformers==4.36.2
accelerate==0.25.0
pillow==10.2.0
imageio==2.34.0
imageio-ffmpeg==0.4.9
